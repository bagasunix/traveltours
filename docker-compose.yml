version: "3.8"
services:
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    restart: always
    environment:
      - POSTGRES_PASSWORD=Kambing04
      - POSTGRES_USER=bagasunix
      - POSTGRES_DB=traveltour
